import {makeVar} from '@apollo/client';

export const user = makeVar([]);

export const favouriteHouses = makeVar([]);

export const allHouses = makeVar({});
export const searchedData = makeVar([]);
export const filteredData = makeVar([]);
export const mapCoords = makeVar([]);